diff --git a/amd64/exception.go b/amd64/exception.go
index 7a1c5ea..6772108 100644
--- a/amd64/exception.go
+++ b/amd64/exception.go
@@ -18,9 +18,20 @@ var (
 	isr        uintptr
 	eip        uintptr
 	isThrowing bool
+	fault      int
 )
 
-func currentVectorNumber() (id int) {
+func Faulty() bool {
+	if fault > 0 {
+		fault = 0
+		return true
+	} else {
+		return false
+	}
+}
+
+// VectorNumber returns the last caught exception/interrupt vector offset.
+func VectorNumber() (id int) {
 	id = int(isr - irqHandlerAddr)
 
 	if id >= 0 {
@@ -39,7 +50,7 @@ func DefaultExceptionHandler() {
 
 	isThrowing = true
 
-	print("exception: vector ", currentVectorNumber(), " \n")
+	print("exception: vector ", VectorNumber(), " \n")
 	exception.Throw(eip)
 }
 
diff --git a/amd64/exception.s b/amd64/exception.s
index 7953e56..d794868 100644
--- a/amd64/exception.s
+++ b/amd64/exception.s
@@ -12,21 +12,40 @@
 TEXT ·handleException(SB),NOSPLIT|NOFRAME,$0
 	CLI
 
+	// save caller registers
+	PUSHQ	AX
+
 	// find ISR offset from stack linking information (see irqHandler)
-	MOVQ	isr+(8*0)(SP), R12
-	SUBQ	$(const_callSize), R12
-	MOVQ	R12, ·isr(SB)
+	MOVQ	isr+(8*0)(SP), AX
+	SUBQ	$(const_callSize), AX
+	MOVQ	AX, ·isr(SB)
+	MOVQ	$1, ·fault(SB)
 
 	// find EIP from handler's stack
-	ADDQ	$(8*2), SP
-	MOVQ	eip+(8*0)(SP), R12
-	MOVQ	R12, ·eip(SB)
+	MOVQ	eip+(8*2)(SP), AX
+	MOVQ	AX, ·eip(SB)
 
-	// call exception handler on system stack (g0)
 	MOVQ	$·SystemExceptionHandler(SB), AX
-	MOVQ	(AX), AX
-	PUSHQ	AX
-	CALL	runtime·systemstack(SB)
+	CMPQ	AX, $0
+	JE	done
+
+	// call exception handler on system stack (g0)
+	//MOVQ	(AX), AX
+	//PUSHQ	AX
+	//CALL	runtime·systemstack(SB)
+	//POPQ	AX
+done:
+	MOVQ	24(SP), AX
+	ADDQ	$2, AX // skip faulty instruction
+	MOVQ	AX, 24(SP)
+
+	// restore caller registers
+	POPQ	AX
+
+	// return to caller
+	ADDQ	$16, SP
+	STI
+	IRETQ
 
 // To allow a single user-defined ISR for all vectors, a jump table of CALLs,
 // which save the vector PC on the stack, is built to use as IDT offsets.
diff --git a/amd64/irq.go b/amd64/irq.go
index 09f24ca..eb1377b 100644
--- a/amd64/irq.go
+++ b/amd64/irq.go
@@ -164,6 +164,6 @@ func (cpu *CPU) ServiceInterrupts(isr func(int)) {
 		// only after we are sleeping.
 		go cpu.ClearInterrupt()
 		<-c
-		isr(currentVectorNumber())
+		isr(VectorNumber())
 	}
 }
diff --git a/arm64/exception.go b/arm64/exception.go
index 348a806..c7885dc 100644
--- a/arm64/exception.go
+++ b/arm64/exception.go
@@ -33,7 +33,19 @@ func (fn exceptionHandler) vector() uint64 {
 	return **((**uint64)(unsafe.Pointer(&fn)))
 }
 
-var isThrowing bool
+var (
+	isThrowing bool
+	fault      int
+)
+
+func Faulty() bool {
+	if fault > 0 {
+		fault = 0
+		return true
+	} else {
+		return false
+	}
+}
 
 // DefaultExceptionHandler handles an exception by printing its vector and
 // processor mode before panicking.
diff --git a/arm64/exception.s b/arm64/exception.s
index e5f3d8c..e9f68ad 100644
--- a/arm64/exception.s
+++ b/arm64/exception.s
@@ -9,9 +9,14 @@
 #include "textflag.h"
 
 TEXT ·handleException(SB),NOSPLIT|NOFRAME,$0
+	STP	(R0, R1), -(1*16)(RSP)
+	MOVD	$1, R0
+	MOVD	R0, ·fault(SB)
 	MRS	ELR_EL1, R0
-	MOVD	R0, 8(RSP)	// arg
-	JMP	·systemException(SB)
+	ADD	$4, R0 // skip faulty instruction
+	MSR	R0, ELR_EL1
+	LDP	-(1*16)(RSP), (R0, R1)
+	ERET
 
 // func set_vbar(addr uint64)
 TEXT ·set_vbar(SB),NOSPLIT,$0
